#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <iostream>

using namespace std;

void pick(short *, short);
void pattern(short *, short, short *, short, short *, short*);
bool SUM(short *, short, short *, short);
void clear(short *, short);
void clear_2(short *, short);
bool comp(short *, short *, short);
#endif // FUNCTIONS_H
